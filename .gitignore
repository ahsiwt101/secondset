# Generated by xcodegen from project.yml — never commit, never hand-edit.
SecondSet.xcodeproj/
*.xcworkspace/
xcuserdata/
DerivedData/
build/
.DS_Store

# Trained reference objects are large (multi-MB), regenerable from the USDZ
# + Create ML project, and land at unpredictable times as training finishes.
# Treated as a build artifact, not source — ignored by default.
*.referenceobject

# Generated by xcodegen from the `info:` block in project.yml.
SecondSet/Info.plist
